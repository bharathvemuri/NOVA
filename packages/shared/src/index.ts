/**
 * Placeholder export. This package will hold cross-cutting contracts
 * (types, constants, zod schemas) shared between the astronomy engine,
 * the renderer and the API. No contracts exist yet — see ticket T-001's
 * scope note: no product code is added by the foundation ticket.
 */
export const SHARED_PACKAGE = '@nova/shared' as const;
