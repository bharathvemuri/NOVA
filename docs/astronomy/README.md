# Astronomy Engine Documentation

Placeholder. This directory documents the astronomy engine's models,
algorithms and coordinate systems once `packages/astronomy` is implemented
(see `docs/002_Astronomy_Domain_Specification.md` for the domain spec this
will be derived from). No content yet — ticket T-001 seeds the directory
only; the astronomy engine tickets populate it.
